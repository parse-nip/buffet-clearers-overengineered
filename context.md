# Project Context

> Auto-generated by context-saver on 2026-04-24

## Overview
Buffet Clearers Overengineered is a data analytics pipeline that processes a Telegram chat export from "SMU New Buffet Clearers" to find patterns in free-food alerts. It performs exploratory analysis, feature engineering, and ML modeling (Gradient Boosting) to predict when and where free food appears, then renders matplotlib charts into PNG files under `out/` and mirrors them to `docs/readme/` for the README gallery.

## Tech Stack
- **Python 3.x** — all scripts
- **pandas, numpy** — data manipulation and numerical ops
- **matplotlib** — visualization with a dark "Zerve" theme (`#1D1D20` background)
- **scikit-learn** — Gradient Boosting classifier and regressor
- **boto3** — optional S3 data loading (Lambda/cloud fallback)

## Directory Structure

```
buffet-clearers-overengineered/
├── run_graphs.py             # Entry point: sets env var, exec's all src scripts in order
├── graph_output.py           # Shared save/close helpers for matplotlib figures
├── requirements.txt          # Python dependencies
├── data/
│   └── result.json           # Telegram chat export (primary input)
├── docs/readme/              # Committed PNG copies for README gallery
│   ├── food_patterns/
│   ├── prediction_model/
│   └── weekly_alerts/
├── src/
│   ├── load_inspect.py       # Step 1: load export, parse messages, print overview
│   ├── feature_extract.py    # Step 2: build food_df with temporal/content/location features
│   ├── food_pattern.py       # Step 3: 5 charts – day, week, hour, monthly, top senders
│   ├── weekly_alert.py       # Step 4: weekly bar chart coloured by month
│   └── prediction_model.py   # Step 5: 3 ML models + 6 visualisation figures
└── guidelines/               # Lab notebooks (reference only, not part of pipeline)
```

## File Analysis

### `run_graphs.py`
- **Language:** Python
- **Purpose:** Main entry point. Sets `BUFFET_GRAPH_ROOT`, creates output subdirectories, then `exec`s the 5 `src/` scripts sequentially into a shared `ns` dict so variables (`food_raw_messages`, `food_df`, etc.) flow between scripts without explicit imports. Writes a `manifest.json` and mirrors PNGs to `docs/readme/`.
- **Key exports:** N/A (standalone script)
- **Dependencies:** `graph_output`, `src/*.py` via exec

### `graph_output.py`
- **Language:** Python
- **Purpose:** Shared utility for exporting matplotlib figures. Reads `BUFFET_GRAPH_ROOT` to determine output directory. Provides three helpers: `save_graph` (save only), `save_and_close_if_exporting` (save + close), `save_export_or_interactive` (save + close, or show interactively when no root is set).
- **Key exports:** `export_root`, `save_graph`, `save_and_close_if_exporting`, `save_export_or_interactive`
- **Dependencies:** `os`, `pathlib`, `matplotlib.pyplot` (lazy import inside functions)

### `src/load_inspect.py`
- **Language:** Python
- **Purpose:** Step 1 — resolves the Telegram JSON export via env var → local file candidates → S3 fallback, parses messages, and prints a dataset overview (schema, field presence %, date range, 5 sample messages).
- **Key exports:** `food_raw_messages` (list of dicts), `food_chat_name` (str), `food_total_count` (int)
- **Dependencies:** `json`, `os`, `urllib`, `pathlib`, `boto3`

### `src/feature_extract.py`
- **Language:** Python
- **Purpose:** Step 2 — consumes `food_raw_messages` from step 1 and builds `food_df`, a DataFrame with 18+ columns including temporal features (hour, day, time bucket), content flags (has_photo, is_food_post, is_cleared), and NLP-extracted locations. Prints a detailed feature summary.
- **Key exports:** `food_df` (DataFrame)
- **Dependencies:** `pandas`, `numpy`, `re`, `collections.Counter`

### `src/food_pattern.py`
- **Language:** Python
- **Purpose:** Step 3 — renders 5 dark-theme charts saved to `out/food_patterns/`: alerts by day of week (bar), week-of-year heatmap, hour×day heatmap, monthly trend line with rolling average, and top-15 senders horizontal bar.
- **Key exports:** None (PNG side-effects)
- **Dependencies:** `pandas`, `matplotlib`, `graph_output.save_and_close_if_exporting`

### `src/weekly_alert.py`
- **Language:** Python
- **Purpose:** Step 4 — aggregates food posts by ISO week, renders a bar chart with bars coloured by dominant month and a month legend, saved to `out/weekly_alerts/`.
- **Key exports:** None (PNG side-effect)
- **Dependencies:** `pandas`, `matplotlib`, `numpy`, `graph_output.save_graph`

### `src/prediction_model.py`
- **Language:** Python
- **Purpose:** Step 5 — trains 3 Gradient Boosting models (binary food-post classifier, hour-of-day regressor, location multi-class classifier) and renders 6 figures: 3 feature-importance charts, confusion matrix, predicted vs actual hour scatter, and food-post probability by hour bar chart.
- **Key exports:** `model_food_clf`, `model_hour_reg`, `model_loc_clf`, `loc_label_encoder`, `sender_encoder`
- **Dependencies:** `pandas`, `numpy`, `matplotlib`, `scikit-learn`, `graph_output.save_export_or_interactive`

---

## Change Log

### 2026-04-24 11:26 — Clean up prediction_model.py
- **Files changed:** `src/prediction_model.py`
- **What changed:** Extracted `_plot_feature_importance(importances, color, title, outfile, xlabel)` helper function; replaced 3 near-identical 12-line feature-importance chart blocks (figs 1–3) with 3 single calls to the helper. All figure parameters, colors, titles, and filenames are identical to the original.
- **Why:** User requested code cleanup with no output changes. Eliminates ~24 lines of duplicated boilerplate.

### 2026-04-24 11:24 — Clean up food_pattern.py
- **Files changed:** `src/food_pattern.py`
- **What changed:** Removed unused `import matplotlib.ticker as ticker` and `import numpy as np`; removed 3 redundant `ax.set_facecolor(VIZ_BG)` calls on charts 1, 4, and 5 that were already covered by the subsequent `style_ax()` call.
- **Why:** User requested code cleanup with no output changes. Charts 2 and 3 retain their explicit facecolor calls since they don't use `style_ax`.

### 2026-04-24 11:23 — Clean up feature_extract.py
- **Files changed:** `src/feature_extract.py`
- **What changed:** Removed unused `datetime` import; removed dead `_location_patterns` list (never referenced by `_extract_locations`); removed dead `_text_lower` local inside `_extract_locations`; wrapped all DataFrame building and feature engineering into `_build_food_df(messages)` function, leaving only the summary print block at module level.
- **Why:** User requested code cleanup with no output changes. All print statements and `food_df` export are identical.

### 2026-04-24 11:22 — Clean up load_inspect.py
- **Files changed:** `src/load_inspect.py`
- **What changed:** Removed unused `Counter` import; extracted path-resolution chain (env var → local files → S3 fallback) into `_load_chat_export()` function, eliminating ~40 lines of module-level imperative code with `_`-prefixed vars.
- **Why:** User requested code cleanup with no output changes. All print statements and exported names (`food_raw_messages`, `food_chat_name`, `food_total_count`) are identical.

### 2026-04-24 15:19 — Add K-means clustering (Step 6)
- **Files changed:** `src/clustering.py` (new), `run_graphs.py`
- **What changed:** Created `src/clustering.py` implementing K-means clustering on food posts using 12-dimensional feature space: cyclical sin/cos encoding of hour, day-of-week, and month, plus boolean and continuous engagement features. Elbow method (second derivative of inertia) selects optimal K automatically over K=2–10. Produces 5 charts saved to `out/clustering/`: elbow curve, cluster profile heatmap, PCA 2D scatter, cluster-by-hour stacked bar, cluster-by-dow stacked bar. Updated `run_graphs.py` to create the `clustering/` output subfolder and add `clustering.py` as Step 6 in the exec pipeline.
- **Why:** User requested K-means clustering analysis using day of week, time of day, and higher-dimensional features.

### 2026-04-24 15:25 — Simplify clustering visualisations
- **Files changed:** `src/clustering.py`
- **What changed:** Narrowed features to temporal-only (hour, day-of-week, month via sin/cos — 6 dims); dropped engagement features, PCA scatter, and standardised profile heatmap. Replaced PCA scatter with an intuitive hour×day jitter scatter (chart 2). Replaced standardised heatmap with a normalised time-bucket profile bar chart showing % within each cluster (chart 3). Kept elbow curve, cluster-by-hour stacked bar, and cluster-by-dow stacked bar. Added a readable console summary table (avg hour, top day, top month per cluster).
- **Why:** User said the previous charts were hard to understand. Removed all abstract axes (PCA, standardised values) in favour of human-readable time labels.

## Prompt History

### 2026-04-24 15:19 — Add K-means clustering
- **User prompt:** "I want you to add a k means clustering algorithm for the buffets using day of weej and time of week and other useful metrics in higher dimenstions too"
- **Files modified:** `src/clustering.py` (created), `run_graphs.py`
- **Summary:** Created full K-means clustering pipeline as Step 6. 12-dimensional feature space with cyclical time encoding. Elbow method selects K. 5 charts output to `out/clustering/`.

### 2026-04-24 15:25 — Simplify clustering
- **User prompt:** "I want clusterings using 2 or more of these metrics: day of week, time of day, month of year, etc right now they are hard to understand make them simpler"
- **Files modified:** `src/clustering.py`
- **Summary:** Replaced PCA scatter and standardised heatmap with hour×day jitter scatter and time-bucket % profile. Features narrowed to temporal-only. All axes now use readable human labels.
